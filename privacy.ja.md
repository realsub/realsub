# RealSub プライバシーポリシー

最終更新:2026-08-13

RealSub は、お使いのコンピューター上だけで動作するリアルタイム字幕ツールです。
**音声認識はすべてローカルで行われ、音声データがあなたのパソコンの外に出ることはありません。**
本ポリシーでは、RealSub が扱うデータ、外部サービスへ送信が発生する場合、そして保存場所について説明します。

## 1. 行わないこと

- **テレメトリ・利用統計・クラッシュの自動送信は一切ありません。** バックグラウンドで当方に情報を送ることはありません。
- **アカウントは不要です。** 登録は不要で、氏名・メールアドレスなどの識別情報を収集しません。
- **マイクは使用しません。** RealSub が取り込むのはシステムで再生中の音声(または指定したアプリの音声)であり、マイク入力は録音しません。
- **広告・プロファイリングは行わず、個人データを第三者に販売・共有することもありません。**

## 2. 音声と字幕

- 音声は Windows のシステムループバック録音(WASAPI loopback)で取得し、メモリ上で直接ローカルの音声認識モデルに渡します。
- 音声認識モデル(Whisper)と音声区間検出モデル(Silero VAD)は**アプリと一緒にローカルへインストール**され、認識にインターネット接続は不要です。
- **音声データがサーバーへ送信されることはなく**、当方に送られることもありません。

## 3. 翻訳(唯一、テキストが外部へ送信される機能)

翻訳機能は既定で有効です。**翻訳が有効な場合、認識された字幕テキスト(テキストのみ・音声は含みません)が、
設定で選択した翻訳サービスへ送信されます。** 選択できるサービスと送信先は次のとおりです。

| 設定の翻訳エンジン | テキストの送信先 | 適用されるプライバシーポリシー |
|---|---|---|
| Microsoft 翻訳(既定) | Microsoft Bing 翻訳サービス(`www.bing.com`) | [Microsoft プライバシー ステートメント](https://privacy.microsoft.com/privacystatement) |
| Google 翻訳 | Google 翻訳サービス(`translate.google.com`) | [Google プライバシー ポリシー](https://policies.google.com/privacy) |
| DeepL | DeepL(ご自身の API キーが必要) | [DeepL プライバシーポリシー](https://www.deepl.com/privacy) |
| OpenAI 互換エンドポイント | **ご自身で入力したアドレス**(互換サービスや自前サーバー) | 当該サービス提供者の方針によります |
| ローカル翻訳 | ご自身のパソコン上で動作する翻訳サーバー(`127.0.0.1`)。**パソコンの外には出ません** | 該当なし |

翻訳リクエストについて:

- リクエストに含まれるのは**翻訳対象の字幕テキストと対象言語のみ**です。アカウント情報・端末識別子・音声は含みません。
- Microsoft と Google については、各社の公開ウェブ翻訳エンドポイントを利用します。リクエストを記録するかどうかは各サービス側の判断であり、当方の管理外です。
- DeepL または OpenAI 互換エンドポイントを選んだ場合、入力した API キーは**ローカルの設定ファイルにのみ保存**され、指定されたサービスの呼び出しにのみ使用されます。
- **翻訳をオフにすると、RealSub は外部サービスへ一切データを送信しません。** 完全にオフラインで動作します。
- 無料版に翻訳機能は含まれないため、無料版の利用中にテキストが外部へ送信されることはありません。

## 4. パソコン内に保存されるデータ

以下はローカルにのみ保存され、アップロードされることはありません。

| 内容 | 保存場所 |
|---|---|
| 設定(表示・モデル・翻訳の設定、入力した API キーを含む) | `%APPDATA%\RealSub\config.toml` |
| 実行状態(ウィンドウ位置、体験期間の起点タイムスタンプなど) | `%APPDATA%\RealSub\state.toml` |
| 動作ログ(不具合調査用。既定設定では字幕テキストを含みません) | `%APPDATA%\RealSub\logs\` |
| 履歴(字幕テキストとタイムライン)と任意の録音(MP3) | `%APPDATA%\RealSub\history\` |

- 履歴と録音は**設定でオフにできます**。保存済みの記録は履歴ウィンドウから削除でき、上記フォルダーを直接削除しても構いません。
- 古い履歴は、設定した保持日数に従って自動的に削除されます。
- アンインストールしても `%APPDATA%\RealSub` は自動削除されません。完全に消したい場合は手動で削除してください。

## 5. Steam 版について

- RealSub は Steam で配信されています。アカウント・購入・プレイ時間などのデータは Steam クライアント側で扱われ、
  [Valve のプライバシーポリシー](https://store.steampowered.com/privacy_agreement/)が適用されます。当方があなたの Steam アカウント情報にアクセスすることはできません。
- RealSub が Steam のインターフェイス経由で読み取るのは、**完全版 DLC を購入済みかどうか**、現在 Steam にログイン中かどうか、
  および Steam サーバー時刻(システム時計の変更に影響されない体験期間の計測用)だけです。
- RealSub は **Steam Cloud** に `trial.dat` というファイルを 1 つ保存します。内容は**初回起動時のタイムスタンプ(数値 1 つ)**のみで、
  別のパソコンでも体験期間が一貫するようにするためのものです。字幕・音声・個人情報は含みません。

## 6. 診断情報のエクスポート

RealSub には「診断情報をエクスポート」ボタンがあります。**あなたがクリックしたときにのみ**ローカルに zip ファイルを作成し、
**自動送信は一切行いません**。当方へ送るかどうかはあなたの判断です。zip には次が含まれます。

- `config.redacted.toml`:設定内容(API キーは `***` に置換済み)
- `state.toml`:ウィンドウ位置や体験期間の計測などの実行状態
- `logs/`:直近 3 セッションの動作ログ(モデル読み込み・デバイス切替・エラーなど。既定設定では字幕テキストを含みません)
- `env.txt`:アプリのバージョン、Windows のバージョン、Python のバージョン、GPU の型番、演算デバイス、ライセンス状態、
  および **CUDA ランタイムライブラリのインストールパス**

上記 4 項目は zip に書き込む前に、**ユーザー名およびそれを含むパスのマスク処理**をまとめて行います。ユーザープロファイルの
ディレクトリは文字列 `%USERPROFILE%` に置き換えられ、残る `C:\Users\ユーザー名\...` 形式のパスは
`C:\Users\***\...` になります。送信前に zip を開いて内容をご確認いただくこともできます。

## 7. 子どものプライバシー

RealSub は 13 歳未満の子どもを対象としておらず、子どもの個人情報を意図的に収集することはありません
(そもそも本ソフトウェアは個人情報を一切収集しません)。

## 8. 本ポリシーの変更

本ポリシーを変更する場合は、このページ冒頭の日付を更新し、リリースノートにも記載します。

## 9. お問い合わせ

プライバシーに関するお問い合わせ先:`realsub.support@gmail.com`(Steam ストアページの開発元連絡先もご利用いただけます)。

— RealSub
