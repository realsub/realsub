# RealSub 隐私声明

最后更新:2026-08-13

RealSub 是一款在你自己的电脑上运行的实时字幕工具。**语音识别全部在本地完成,音频永远不会离开你的电脑。**
本声明说明 RealSub 会处理哪些数据、什么情况下会有数据发往外部服务,以及这些数据保存在哪里。

## 1. 我们不做的事

- **没有遥测、没有使用统计、没有崩溃自动上报。** RealSub 不会在后台把任何信息发回给我们。
- **没有账号系统。** 不需要注册,不收集你的姓名、邮箱或任何身份信息。
- **不使用麦克风。** RealSub 采集的是系统正在播放的声音(或你指定的某个应用的声音),不录制麦克风输入。
- **不做广告、不做用户画像、不向任何第三方出售或共享个人数据。**

## 2. 音频与字幕

- 音频通过 Windows 的系统回环采集(WASAPI loopback)读取,直接在内存中送入本地语音识别模型。
- 语音识别模型(Whisper)与语音活动检测模型(Silero VAD)都**随程序一起安装在本地**,识别过程不需要联网。
- **音频数据不会上传到任何服务器**,也不会被发送给我们。

## 3. 翻译(唯一会把文本发往外部的功能)

翻译功能默认开启。**开启翻译时,识别得到的字幕文本(仅文本,不含音频)会被发送到你在设置里选择的翻译服务**,
以取回译文。可选的服务及其数据接收方:

| 设置中的翻译源 | 文本发往 | 适用的隐私政策 |
|---|---|---|
| 微软翻译(默认) | 微软 Bing 翻译服务(`www.bing.com`) | [Microsoft 隐私声明](https://privacy.microsoft.com/privacystatement) |
| 谷歌翻译 | Google 翻译服务(`translate.google.com`) | [Google 隐私政策](https://policies.google.com/privacy) |
| DeepL | DeepL(需你自行填写 API 密钥) | [DeepL 隐私政策](https://www.deepl.com/privacy) |
| OpenAI 兼容接口 | **你自己填写的服务地址**(可以是任意兼容服务或你自建的服务) | 由该服务的提供方决定 |
| 本地翻译 | 你本机上运行的翻译服务(`127.0.0.1`),**不出本机** | 不适用 |

关于翻译请求的说明:

- 请求中只包含**待翻译的字幕文本**与目标语言,不含账号信息、不含设备标识、不含音频。
- 对于微软/谷歌两档,RealSub 使用它们的公开网页翻译接口;是否记录请求由这些服务自行决定,不受我们控制。
- 选择 DeepL 或 OpenAI 兼容接口时,你填写的 API 密钥**只保存在本机配置文件里**,只用于向你指定的服务发起请求。
- **关闭翻译开关后,RealSub 不会向任何外部服务发送任何数据**,可以完全断网运行。
- 免费版不提供翻译功能,因此免费版在使用中不会有任何文本外发。

## 4. 保存在你电脑上的数据

以下数据只保存在本机,不会被上传:

| 内容 | 位置 |
|---|---|
| 配置(界面/模型/翻译设置,含你填写的 API 密钥) | `%APPDATA%\RealSub\config.toml` |
| 运行状态(窗口位置、试用起算时间戳等) | `%APPDATA%\RealSub\state.toml` |
| 运行日志(用于排查故障,默认配置下不含字幕内容) | `%APPDATA%\RealSub\logs\` |
| 转录历史(字幕文本与时间轴)与可选的录音(MP3) | `%APPDATA%\RealSub\history\` |

- 历史记录与录音是**可以在设置里关闭**的功能;已有记录可以在历史窗口里删除,也可以直接删掉上述目录。
- 程序会按你设置的保留天数自动清理旧的历史记录。
- 卸载程序不会自动删除 `%APPDATA%\RealSub`,如需彻底清除请手动删除该目录。

## 5. Steam 版本相关

- RealSub 通过 Steam 发行。Steam 客户端本身会处理你的账号、购买与游玩数据,这部分由
  [Valve 隐私政策](https://store.steampowered.com/privacy_agreement/)约束,我们无法访问你的 Steam 账号信息。
- RealSub 通过 Steam 接口读取的信息仅限于:**是否已购买完整版 DLC**、当前是否已登录 Steam、以及 Steam 服务器时间
  (用于试用期计时,防止改系统时间)。
- RealSub 会在 **Steam Cloud** 中保存一个文件 `trial.dat`,内容只有**首次运行的时间戳**(一个数字),
  用于让试用期在你换电脑后保持一致。其中不含任何字幕、音频或个人信息。

## 6. 诊断信息导出

RealSub 提供「导出诊断信息」按钮,**仅在你手动点击时**生成一个本地 zip 文件,**不会自动发送到任何地方**。
是否把它发给我们完全由你决定。该 zip 中包含:

- `config.redacted.toml`:你的配置,其中 API 密钥已被替换为 `***`;
- `state.toml`:窗口位置、试用计时等运行状态;
- `logs/`:最近 3 个会话的运行日志(记录模型加载、设备切换、错误等,默认配置下不含字幕文本);
- `env.txt`:应用版本、Windows 版本、Python 版本、显卡型号、运行设备、许可状态,以及 **CUDA 运行库的安装路径**。

上述四项在写入 zip 前都会统一做**用户名与包含它的路径的脱敏**:你的用户目录会被替换为字面 `%USERPROFILE%`,
其他形如 `C:\Users\你的用户名\...` 的路径会被打码为 `C:\Users\***\...`。发送前你仍可以先打开 zip
检查内容。

## 7. 儿童隐私

RealSub 不面向 13 岁以下儿童设计,也不会有意收集儿童的个人信息(事实上本软件不收集任何个人信息)。

## 8. 声明变更

本声明如有变更,我们会更新本页顶部的日期,并在版本更新说明中注明。

## 9. 联系方式

有隐私相关问题,请联系:`realsub.support@gmail.com`(也可通过 Steam 商店页的开发者联系方式)。

— RealSub
